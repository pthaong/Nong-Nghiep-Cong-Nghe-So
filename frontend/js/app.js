/*
 * AgriSmart application entry point.
 * The complete working implementation is kept in app-core.js to preserve behavior.
 * Functional modules are extracted under ./modules/ for maintainability.
 *
 * IMPORTANT: app-core.js is loaded first so the current application behavior remains unchanged.
 */
