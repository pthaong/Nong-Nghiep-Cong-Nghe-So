/* Backend configuration.
   Set API_BASE_URL when the real backend is available.
   Example: window.APP_CONFIG = { API_BASE_URL: "http://localhost:8080/api" };
*/
window.APP_CONFIG = window.APP_CONFIG || {
  API_BASE_URL: ''
};
